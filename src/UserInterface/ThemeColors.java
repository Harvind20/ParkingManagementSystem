package UserInterface;
import java.awt.Color;

// central place to store theme colors used across all UI screens
public class ThemeColors {
    public static final Color PRIMARY = new Color(2, 52, 63);      // dark teal background
    public static final Color SECONDARY = new Color(240, 237, 204); // light cream panels/buttons
}
